# Cineloom remote generation server.
# torch / diffusers / transformers come from the modelbase/ai-base:cu128-torch28
# base image (verified). Do NOT pin torch here.
#
# The base image is missing a few pieces of the verified LTX-2.3 recipe, added here:
#   sdnq                 int8 quantizer (required to load the distilled model)
#   opencv / imageio     export_to_video back-ends
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.33
python-multipart>=0.0.9
pydantic>=2.5,<3

# --- verified LTX-2.3 runtime pieces missing from the base image ---
sdnq==0.2.0
opencv-python-headless
imageio
imageio-ffmpeg
